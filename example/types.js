//@flow

/*::

export type A = {|
    num: number,
    str: string,
    bool: boolean,

    numLit: 1 | 20,
    strLit: 'a' | 'bc',

    numOpt?: number,
    numNull: ?number,
|};

export type B = {|
    arr: A[],
    tuple: [string, number, 1 | 2],
    c: string | number,
    d: boolean | null,
    e: {|
        a: 1,
        b: string,
    |} | {|
        a: 2,
        b: number,
    |},
    f: {[zz:string]: number|string},
|};

*/
